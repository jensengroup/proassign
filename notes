bmrb har statestik på kemiske skift af alle slags for alle aminosyrer. Det skal vel begrænse udvalget somehow. Det burde være muligt at assigne næsten alle eksperimentelle CS baseret på dette.

stene lidt mere over sandsynligheder og scaling

Tænke over en måde at lave en initial assortment.
evt bruteforce f(N,s,x)
forskellen mellem at udregne energi af alle, og så skifte. Og mellem at skifte for hver negativ energi
evt kun tage ikke assignede punkter per iteration.


fucker det op fordi man bruger random numbers til histogram. Hvis man kun gør det balanced, hvordan ser det så ud.

Det ser ud til at min ikke altid findes ved komplet randomization.

kT forsvinder i bolzmann fordelingen? 

regn max s og p x ud for balance metode

histogram can return multiple of the same position e.g. [7, 1, 2, 3, 4, 5, 6, 7, 8, 9]

need to make the time the assigner runs consistent or at least a bit prettier. x2

make str file output

make cam files

smid anders' c++ ind.

kig på troels' data.

læs shiftx2 hvordan de har fået std

kig på artikler anders sendte long tiem ago.

plot der viser energien i forhold til 0 i kT sammenlignet med hvor mange den finder. Evt variansen af energien.
str'ish fil. Viser % for den vi fandt vs den rigtige.
histogram over fordelingen for testprotein.
script assumes that in the str, H, C, CA etc is correctly assigned relatively, e.g. for H_i, C_j, CA_l it is assumed that i=j=l
Show how test protein % scales with CS
scale with res size. (Maybe just for synthetic data)
explain camshift, shiftx2 and mahir's
